# Panel cell: llama31-8b x math500  (independent standard family; GATED, needs HF token)
# CONFIRMATORY. Parameters are fixed by the OSF registration osf.io/8w2q3
# (plan: docs/preregistration.md at tag prereg-v1.0, SHA-256 036288be...873cd).
# Do not edit sampling, segmentation, monotonicity or seed: they are registered.
backend: vllm
model:
  name: meta-llama/Llama-3.1-8B-Instruct
  revision: "0e9e39f249a16976918f6564b8830bc894c89659"
  quantization: none          # fp16 across the whole panel (registered)
dataset:
  name: math500
  split: test
  limit: 500
sampling:
  m: 5
  temperature: 0.7
  max_tokens: 150
segmentation:
  strategy: blank_line
  window_tokens: 40
  max_steps: 8
monotonicity:
  eps: 0.01
analysis:
  n_boot: 1000
vllm:
  gpu_memory_utilization: 0.9
  max_model_len: 4096
  dtype: bfloat16          # pinned, not 'auto': the registration requires identical
                          # precision across the panel, and 'auto' defers to each model's config
run:
  seed: 0
  name: llama31-8b-math500
